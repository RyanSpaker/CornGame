# Corn Todo:
- Allow corn fields to have multi step init methods. For example running a compact algorithm after initializatin to defragment image carved fields
- determining number of lods from file instead of using a const variable in the code
- change readback to use tracing messages instead of println
- Make a custom vertex shader
- Setup vertex attributes to allow mesh pieces to be different colors
- Setup some sort of stencil operation to allow texture sampling for the corn to be deferred
- Setup timing for the gpu tasks
- Remake vote-scan-compact so that Corn data is generated there every frame isntead of in init shaders